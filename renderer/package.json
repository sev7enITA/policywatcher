{
  "name": "policywatcher-renderer",
  "version": "1.0.0",
  "private": true,
  "description": "Headless-browser rendering service for the PolicyWatcher scraper (deploy on a VPS)",
  "type": "module",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs",
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.50.0",
    "tldts": "^7.4.6"
  },
  "engines": {
    "node": ">=20"
  }
}
